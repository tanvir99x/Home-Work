// Arrow function COMPACT way to write function

function sum(a, b) {
    return a + b;
}

function mal(a, b) {
    return a * b;
}
// see  function write simpla or compact stracture modern JAVASRIPT.

const arr = (a, b) => { /*arr is function not variable*/
    console.log(a * b);
};
// we can doing -,+,*,/ aslo same stracture
// arrow func use small multification only 

// arrow function showing output without input like =>

const printHELLO = () => { /*when have onle line we can remove {} */
    console.log('Hello');
}