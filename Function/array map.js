// map method forEach similer
// map method goes ararys one by one and perform operation
// map method create new array from old arrays.
let nums = [1, 2, 3, 4, 5, 6];
let arri = nums.map((val) => {
    return val; /*we can multiple own value in array like 2*2*/
});
console.log(arri);